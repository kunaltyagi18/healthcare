# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
dist-ssr
*.local

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?

# ── Image Migration Pipeline ─────────────────────────────────────────────────
# Raw zip sources (often huge, long filenames that break Windows paths)
/raw-zips/

# Extracted images (re-generated by --extract; not tracked)
/raw-new-images/

# Secret credentials
.env
.env.*

# Pipeline progress & failure logs
scripts/upload-progress.json
scripts/upload-failures.json
scripts/extract-failures.json
scripts/filename-mapping.json
scripts/match-report.json
scripts/prelaunch-audit-report.json

# Backups created by --finalize
scripts/backups/

# Intermediate updated products file (reviewed before finalize runs)
public/data/products-updated.json
